export default (inind) => {
    const [answer, rightAns] = inind;
    if (answer === rightAns){
        console.log ('Correct!');
        return true;
    } else {
        console.log('"'+ answer + '"'+ ' is wrong answer ;(. Correct answer was ' + '"' + rightAns + '".');
        return false;
    };
}